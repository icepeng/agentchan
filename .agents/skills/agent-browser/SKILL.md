---
name: agent-browser
description: Browser automation CLI for AI agents. Use when the task requires navigating pages, filling forms, clicking buttons, taking screenshots, scraping data, testing the web app, or checking browser-visible behavior.
---

# agent-browser

Use the shared skill body in `.claude/skills/agent-browser/SKILL.md`.

For Codex, load that file before running browser automation commands. Prefer the
installed CLI's live instructions when available:

```bash
agent-browser skills get core
```

If browser launch or GUI access requires approval in the current sandbox, request
approval before running the command.
