/// <reference lib="dom" />

/**
 * iframe shell entry placeholder. Slice 3 of PRD #176 will replace this
 * with the Comlink + RendererShell adapter that boots inside the iframe
 * document. Kept as a typed module so package.json `exports` can list the
 * subpath today and resolve cleanly in editors.
 */

export {};
