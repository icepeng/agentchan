# Plain HTTP for the portless proxy in dev. Forced project-wide so a user-level
# `portless proxy start` with default HTTPS doesn't conflict with `bun run dev`.
# See AGENTS.md for the rationale.
PORTLESS_HTTPS=0
